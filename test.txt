this is a test text file:

git commands:

git init / git init <dirname>
git log
git status
git add => add files to staging dir
ls -lrt => gives file names in working dir
git commit -m "test message" - saves files to local repo (now working dir is clean)
ls -la => gives all files in working dire including git files
git diff => gives the differences in files in working dir and staging dir
git diff --staged => give the differences in files in staging dir and local repo

//git revert changes:



added new text to modify a file